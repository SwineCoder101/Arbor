"use client"

// This component is not needed anymore since we're using direct DOM manipulation
// for toast notifications. Keep this file as a placeholder to avoid import errors.
export function Toaster() {
  return null
}